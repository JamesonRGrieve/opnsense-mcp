# SPDX-License-Identifier: AGPL-3.0-or-later
PLUGIN_NAME=		mcpserver
PLUGIN_VERSION=		0.1
PLUGIN_REVISION=	0
PLUGIN_COMMENT=		MCP Server for AI-assisted firewall inspection (read-only)
PLUGIN_MAINTAINER=	jamesonrgrieve@gmail.com

# This Makefile follows the OPNsense plugins convention. When built inside the
# opnsense/plugins tree, the shared Mk/plugins.mk handles packaging. Outside
# that tree (standalone repo), it serves as structural documentation.
# .include "../../Mk/plugins.mk"
